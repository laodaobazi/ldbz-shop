app:
  id: ldbz-shop-service-item
apollo:
  meta: http://192.168.136.133:8080

spring:
  dubbo:
    server: true
  datasource:
    druid:
      filter:
        config:
          enabled: true

logging:
  level:
     cn.ldbz.mapper: debug
     
# 静态页面生成目录
static_html_path: /opt/ldbz-shop/ldbz-shop-service-item/item/
